# CHANGELOG — loco_state

本卡的版本变更记录(倒序)。每次更新必加一条,并同步递增 `metadata.json` 的 `version`。

## v1.0.0 — 2026-07-14 — luoye-hcl

- 首次收录。Go1 高层运动状态只读传感器卡,10Hz,`/{ns}/loco/state`。
- 实机验收通过(站立/阻尼模式随真实状态切换,core 网页数据流正常)。
