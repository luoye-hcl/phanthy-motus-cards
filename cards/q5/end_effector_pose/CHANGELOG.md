# CHANGELOG — end_effector_pose

本卡的版本变更记录(倒序)。每次更新必加一条,并同步递增 `metadata.json` 的 `version`。

## v1.0.0 — 2026-08-13 — huangchanglong

- 新增本卡。数据源 `/mobile_manipulator/end_effector_pose`(`std_msgs/msg/Float32MultiArray`)真机已验证有数据。
- 驱动插件待实现(参考骨架见 `impl/end_effector_pose.py`)。
- 状态 `draft`。
