# CHANGELOG — simple_action

本卡的版本变更记录(倒序)。每次更新必加一条,并同步递增 `metadata.json` 的 `version`。

## v1.0.0 — 2026-08-13 — huangchanglong

- 以驱动 `robotera-q5-driver` 的 simple_action.py 为准收录本卡(该插件已实现但 config.yaml disabled)。
- 白名单动作:zero / initpose_handsdown / lift_up。
- 状态 `draft`(需现场解锁后验收)。
