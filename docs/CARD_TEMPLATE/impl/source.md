# 实现来源(source)

- **源仓库**:`<owner/repo>`
- **文件路径**:`<path/to/file.py>`
- **类名**:`<ClassName>`
- **公共依赖**:`<基类/控制核心/公共层>`
- **注册位置**:`<在哪个文件按什么条件注册>`
- **运行载体**:驱动镜像 `<image>`,MCP 端口 `<port>`

> 本目录 `impl/` 下的源码为**摘录**,便于审核理解这张卡如何产出返回值;不保证脱离真实驱动可独立运行。以源仓库为准。
> ⚠️ 严禁把明文口令、密钥、内网私密地址拷入本目录。
